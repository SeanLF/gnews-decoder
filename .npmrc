# No lifecycle scripts on install: a dependency cannot run code on a developer machine or in CI.
# Explicit `npm run <x>` still works.
ignore-scripts=true
# Resolve only versions published at least 7 days ago, the same cooldown Dependabot waits; most
# compromised releases are pulled within that window.
min-release-age=7
